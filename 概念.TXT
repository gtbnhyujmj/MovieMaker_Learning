直接把文字渲染在網頁上，再載入colab，會不會比較快????

用DJANGO的css/JS寫網頁渲染，然後看是要存檔，還是直接有辦法壓印在影片上，或 投影到影片上都可以。這樣可以同時學網頁也可以學製作影片。一舉兩得。

cv2 是處理影片、圖片的工具。
PIL.Image 是處理圖片的另一種方式，特別是 PNG 貼紙常有透明背景。
numpy 用來快速處理畫面上每一個像素的數學運算（例如疊圖時透明度計算）。
